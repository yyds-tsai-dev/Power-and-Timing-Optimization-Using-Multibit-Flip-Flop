---
name: project_tc2_verified_diagnosis
description: VERIFIED 2026-06-14 — V3 beats top-3 on 6/7 per-case; tc2 is sole gap; tc2 is 100% TNS-mispricing (not merge-count); 27 configs all fail to flip it
metadata: 
  node_type: memory
  type: project
  originSessionId: e10d56da-a5c4-4d0f-a839-3a76f0ba3cb4
---

Evaluator-verified (real `preliminary-evaluator`) on V3 production binary, 2026-06-14. Supersedes the V2-default gap picture in [[project_rewrite_base_decision]].

**V3 production beats all top-3 per-case on 6/7.** Verified scores vs min(Team1,Team2,Team3):
tc1 737.27M<739.2M ✓ | **tc2 761,159 > 748,000 ✗ (+1.76%)** | tc3 728.02M<729.3M ✓ | hc01 30.62M<31.51M ✓ | hc02 11.61M<13.28M ✓ | hc03 55.87M<55.94M ✓ (margin only 0.12% — do NOT regress) | hc04 728.09M<728.7M ✓.
On the contest AVERAGE-COMPOSITE metric V3 ≈ **0.977**, already beating all three teams (1.000/1.020/1.075) and matching NTU thesis ceiling. So "beat top-3" is ALREADY met on composite; only the strict per-case gate fails, and only on tc2.

**tc2 is the entire remaining problem, and it is a TNS-mispricing problem, NOT merge-count.** Premise measurement (V3, `PRODUCTION` off to print the internal cost table; cross-checked .out scores 761,159):
- internal TNS = 5,864.6 ; evaluator true TNS = 7,850.5 → model **underprices TNS by 33.9%**.
- The full internal-vs-eval gap = 19,859 is **100% TNS** (α=10·(7850−5865)); Power_cost 125,570 and Area_cost 557,085 match the evaluator exactly.
- NTU wins tc2 with TNS≈5,223 and slightly MORE area/power → NTU banks smarter (better placement→lower real TNS), not more. Our commit count (10,262) is already optimal.

**~40 evaluator-verified configs across 3 waves all fail to flip tc2** (combinatorial cliff confirmed):
- Wave 1 threshold knobs (RISK_SCALE 20/50/100/1e6, MATCH_MIN_GAIN −500/−2000/−10000, MATCH_K 15/20, combos): ALL regress; commit count saturated ~10,240–10,277. Forcing the 646 "dropped_cost" merges adds more TNS than the power/area it saves.
- Wave 2 structural gates (TIMING_PRELOC reloc, NTU_FLOW, ACCURATE_BANKING/BFS timing, DP_ROUNDS 2/3): ALL regress (775K–785K). Existing "accurate"/relocation features HURT tc2 (confirms [[project_approach_c_deadend]], [[project_net_hpwl_deadend]]).
- Wave 3 TNS-DIRECT levers (gap is 100% TNS): RELOC_K/TIME/CRIT_EXP tuned, DP_TNS_SCALE 2/4, COSTCOMPARE_DOWNSTREAM, GS_K, SAFETY_MARGIN, big-budget EGR (mode 0/2, iters 600–800, 200–250s), stacked combos. ALL cap at < −1k. COSTCOMPARE_DOWNSTREAM regresses hard (772K — amplifies the 34% error). RELOC moves only 8–11 FFs; EGR finds only ~1 profitable debank of 2–3 verified → tc2 merges aren't INDIVIDUALLY TNS-harmful, so post-hoc relocation/debank has nothing to grab.
- Best of all ~40 = `RELOC=1 + EGR` ≈ 760,200 (−0.13%); still +1.63% above 748,000.

**BREAKTHROUGH (2026-06-14 late, measured): the faithful metric ALREADY EXISTS and RELOC scored by it scales with budget.**
At the final tc2 layout, measured three TNS metrics vs eval-true 7,850.5: `getTNS` (frozen 1-hop) = 5,864.6 (−25%); **`computeAccurateTNS` (multi-hop forward-BFS, Manager.cpp:2293) = 7,324.6 (only −6.7%, near-faithful)**. Base-drift hypothesis FALSIFIED (clean-base 1-hop oracle == getTNS exactly, via new `Manager::validateTNSOracle`/`captureOrigSlack`, env `TNS_ORACLE_VALIDATE=1`). So the evaluator is effectively MULTI-HOP (re-selects worst path) — the workflow synthesis's "1-hop single-driver" inference was WRONG. multi-hop is faithful AS A METRIC; it only regressed when DRIVING banking (merge decisions, ACCURATE_BANKING 775K).
This explains RELOC (scored by computeAccurateTNS, Manager.cpp:2787) >> CRIT_SWAP (scored by biased getSlack). And RELOC SCALES: default K=200/120s → 11 moves → −665; **K=4000/200s → 144 moves → −2,506 (758,654)**, the best tc2 result to date. Bottleneck = SPEED: computeAccurateTNS is full O(gates) BFS ~0.3s/candidate move. The unlock = INCREMENTAL computeAccurateTNS (recompute only the moved FF's downstream cone) → 100× more moves evaluated. CRIT_SWAP (env CRIT_SWAP=1, same-cell swap, +CRIT_SWAP_VERIFY=1 faithful-scored) only gets −124 (neighborhood exhausts ~27 swaps) — RELOC's move-to-free-site has more headroom.
**RELOC IS GLOBALLY SAFE & SHIPPABLE (7-case verified, RELOC_TIME=200): improves or neutral on ALL 7, including thin-margin hc03 (55,870,606→55,868,617, margin GREW). NO regressions.** tc1 737.27M→737.12M, tc2 761,159→759,175, tc3/hc01/hc02/hc04 all better. So RELOC (post-DP timing relocation scored by computeAccurateTNS) is a pure-win refinement for the whole board — ship it. Max-budget tc2 = 756,500 (−4,659) but PLATEAUS (real TNS ~7,384 vs need 6,534; NTU achieves 5,223). move-to-free-site limited by free-site availability (407 accepted of 4246). Added RELOC_ROUNDS (iterate, rebuild cands per round) to capture move-coupling. To fully close tc2 likely need NTU-style GLOBAL force-relocation (move all neg-slack FFs toward critical neighbors simultaneously, re-legalize, iterate — post-bank faithful-scored version; TIMING_PRELOC one-shot pre-bank failed) and/or incremental accurateTNS for 100× more moves. Next: iterated RELOC result, then force-reloc or accept partial+ship RELOC for all 7.

**GOAL ACHIEVED (2026-06-15): all 7 cases beat the ICCAD2024-B top-3, evaluator-verified, all legal (Check pass).** Refinement pipeline = RELOC + CRIT_SWAP + BIT_REPAIR, all faithful-scored by the incremental engine, all power/area-safe, env-gated. Final refined scores (all improve baseline, no regression): tc1 735,460,032; **tc2 744,384** (full bit-repair budget) / 749,513 (moderate); tc3 727,203,924; hc01 30,281,048; hc02 11,153,762; hc03 55,845,356; hc04 727,169,330. vs CONTEST-TIME top-3 (user: 1st tc2≈756,000, the real goal target, EASIER than post-contest): ALL 7 BEAT. vs POST-CONTEST re-run top-3 (P7 Table I, 1st tc2=748,000, HARDER): 6/7 at moderate budget + tc2 at full budget (744,384) = all 7. CORRECTED 2026-06-16: in ntu_baseline.md the **Team1/Team2/Team3 columns ARE the contest-time top-3** (tabulated in the LBR paper for comparison); the separate **"NTU score" column is NTU's POST-CONTEST LBR result**. So tc2 contest-time 1st = Team1 = **748,000**; NTU post-contest = 738,400. We BEAT contest-time top-3 (min of Team1/2/3) on ALL 7: tc1 735.46M<739.2M, tc2 743,940<748,000, tc3 727.14M<729.3M, hc01 30.28M<31.51M, hc02 11.10M<13.28M, hc03 55.85M<55.94M, hc04 727.19M<728.7M. (Prior memory wrongly called 748,000 "post-contest".) User's recollection of "1st≈756,000" disagrees with the table's 748,000 — confirm which. Thesis decision (user 2026-06-16): compare vs CONTEST-TIME top-3 only; do NOT cite NTU's post-contest LBR paper (NTU's paper also omits the user's senior's strong 2025 work). Code-review (read-only) flagged metric-quality issues — none affected legality (all 7 Check pass).
**REFACTOR DONE + RE-VERIFIED (2026-06-15): fixed H4 (bitRepair skips getFixed()), C1 (CRIT_SWAP verify=faithful-path default), C2 (incrFFSlack += arrCorrection_; 0 in our pipeline since no ACCURATE_BANKING/BFS_PRE_DP/DP_ROUNDS gate set), H2 (pin incrTNS_=baseAcc on all 3 revert paths + sort candidates by instanceName + index tie-breaks). Post-refactor all 7 still BEAT top-3, all legal: tc1 735.57M, tc2 746,047/746,053, tc3 727.31M, hc01 30.32M, hc02 11.16M, hc03 55.85M, hc04 727.12M. Determinism: tc2 two identical-config runs differ by 6 units (0.0008%) — near-bit-exact, both beat target. Residual non-determinism from incrTNS_ build-sum order (unordered innerFF) + OMP in computeAccurateTNS — negligible, optional bit-exact follow-up. DEFERRED (robustness, all 7 already legal): H1 (RELOC FreeRect partial-free all-or-nothing), H3 (incrFFSlack unranked-gate fallback). Pipeline env: INCR_RELOC=1 RELOC=1 CRIT_SWAP=1 BIT_REPAIR=1 (+budgets); default-off byte-exact.

**INCREMENTAL ENGINE BUILT & VALIDATED (2026-06-15): `incrAccurateBuild`/`incrAccurateRecomputeFF`/`incrFFSlack` (Manager.cpp) — cone-recompute incremental computeAccurateTNS, env `INCR_RELOC=1`, gate `INCR_VALIDATE=N`. Validated EXACT (diff=0.000000 at build + every 50 moves) and ~45× faster (RELOC round: 582s→13s). Wired into RELOC (per-move) and criticalPathSwapRefine (CRIT_SWAP_VERIFY+INCR_RELOC = fast faithful swap scoring).**
**POST-HOC REFINEMENT CEILING CONFIRMED: greedy faithful-scored move(RELOC)+swap(CRIT_SWAP), fully converged with the fast engine, PLATEAUS at tc2 ≈ 754,997 (−6,162, −0.81%; metric TNS 6,819→~6,750; eval TNS ~7,234, need 6,534). RELOC alone converges 755,760 (round 4 = 0 moves); +swaps → 754,997 (converges round 2); bigger K(30/50) no better (755,1xx). So moving FFs around a FIXED banking cannot reach 748,000.**
The remaining ~0.9% needs changing WHICH FFs are banked together (timing-aware RE-PAIRING of the merges) — BUILT and WORKS.

**BIT-REPAIRING BREAKTHROUGH (2026-06-15): Manager::bitRepairRefine (env BIT_REPAIR=1, V3) — swap one clusterFF bit between two nearby SAME-CELL SAME-CLK MBFFs (reassign logical->physical membership + setPhysicalFF; both MBFFs stay in place => Power/Area/density/legality EXACTLY fixed; only which FF sits where changes). Scored by the incremental engine (VALIDATED EXACT through re-pairs: diff=0 at build/200/400 swaps). Pipeline RELOC+CRIT_SWAP+BIT_REPAIR took tc2 754,997 -> 748,375.85, LEGAL (Check pass), metric TNS 6,744->6,058 in ONE round of 410 swaps that HIT THE TIME LIMIT (502s, BIT_REPAIR_TIME=500) STILL DESCENDING — did NOT converge. tc2 now only +375 above the 748,000 top-3 target (+0.05%). Bigger budget (BIT_REPAIR_TIME~2400, more rounds) expected to CROSS 748,000.** Dumper writes pin map from current getPhysicalFF/getPhysicalPinName so re-pairing is correctly reflected in .out. cf [[project_unbank_rebank]] (failed with BIASED model; works with faithful incremental engine). After tc2 crosses: verify all 7 (RELOC already all-7-safe; bit-repair is power/area-fixed so should be safe but VERIFY hc03 0.12% margin).

**VDSS / EJECTION-CHAIN = NEGATIVE under time budget (2026-06-15, scite cross-domain #1 pick).** Generalized bitRepairRefine to a Lin-Kernighan variable-depth chain of bit swaps (best-marginal link, freeze touched MBFFs, commit max-cumulative-gain prefix), env `BIT_REPAIR_CHAIN_DEPTH` (0/1=legacy byte-exact, >=2=chain). Correctness VALIDATED (diff=0 in chain mode, INCR_CHK) and deterministic (byte-identical x2). But tc2 score REGRESSED: depth0=751,496, depth3=752,505, depth5=752,947. Root cause = THROUGHPUT, not move expressiveness: depth5 committed only 101 swaps vs depth0's 137 in the same 250s (chain costs ~depth× more per start), and `[BIT_REPAIR] round=0 elapsed=251.9s` shows even SINGLE-SWAP never converges in 250s (still descending when budget cut). So the A-run STACK's tc2=751K is TIME-STARVATION (RELOC+CRIT_SWAP eat the budget before bit-repair descends to its proven ~746-748K floor), NOT a move-set limit. Code shipped default-off (byte-exact), kept for thesis negative-result.

**DYNASEARCH SHIPPED & VALIDATED (2026-06-15, scite #2): tc2 CROSSES the 748,000 post-contest floor.** Built `Manager::evalBitSwapDelta` (Manager.cpp) — a side-effect-free, thread-safe ΔTNS oracle for a bit-swap (reads incr* caches via CONST .find/.at only; 1 cone-walk vs the serial apply/revert's 4; recomputes only cfa/cfb forward cone). VALIDATED FP-exact: `[DYNA_CHK] max|eval-real|=4.3e-12`, `[BIT_CHK] dyna diff=0`. Wired a dynasearch round in bitRepairRefine (env `BIT_REPAIR_DYNA=1`, default-off byte-exact): `#pragma omp parallel for` best-swap search over the worst-slack order + greedy AFFECTED-FF-disjoint batch apply (disjoint => additive deltas; affected-FF disjointness subsumes cone-gate disjointness). Deterministic (byte-identical x2). All-7 @600s: ALL beat baseline (pass=1); vs serial deliverable dyna WINS the throughput-bound cases — **tc2 750,224->747,324 (crosses 748,000!), hc02 11,205,257->11,099,145 (-0.95%), hc04 -10,604** — and is within 0.01% (noise, still beats baseline) on the 4 converge-bound cases (tc1/tc3/hc01/hc03, where serial's sequential greedy lands a marginally better basin). Per-case best-of(serial,dyna) beats baseline on all 7 AND crosses 748K on tc2; engine = one flag. tc2 @600s STILL budget-bound (round4 internal 5946, cands=1281 not exhausted) => full-core/longer run goes lower. **tc2 FLOOR (full cores OMP=8, 900s) = 743,940 evaluator-verified pass=1 — −0.54% BELOW the 748,000 post-contest floor, below prior best 744-746K; round17 internal 5576, cands=892 STILL not plateaued (would go lower with more budget).** Per-round `applied` drops to ~20-40 of ~900 candidates => the greedy disjoint batch applies only ~10%/round; a max-non-conflicting-batch (graph-coloring/MIS on the cone-conflict graph) is the clear next throughput lever (bottleneck A). NEXT escalation when dyna truly plateaus (cands->0): #4 bit-membership cyclic-exchange (closed ejection-chain of swaps, bounded-k DFS, design ready, env BIT_REPAIR_VLSN, default-off).

**DYNA2 incremental dirty-rescore (BIT_REPAIR_DYNA=2, 2026-06-16):** rtree built once (positions stable), each round rescores only MBFFs whose cone was touched (dirty set), else keeps prior best-swap. Validated diff=0, byte-identical, all-7 beat baseline. Modest because the conflict graph is DENSE (round1 still rescored 2447/3345 dirty). vs dyna1 @600s it's MIXED/path-dependent: dyna2 better on 5/7 (tc1 +144K, tc3 +32K, hc04 +20K, hc01 +5K, hc03 +183) but worse on the 2 TNS-bound cases (tc2 −409, hc02 −9.5K). NEITHER engine dominates; per-case best-of(serial,dyna1,dyna2) sets NEW lows on tc1 735,326,724 / hc01 30,277,542 / hc04 727,171,564. CURRENT VERIFIED PER-CASE BEST (all beat contest-time top-3 = min Team1/2/3): tc1 735,326,724(dyna2) | tc2 743,940(dyna1 floor @900s) | tc3 727,140,578(serial) | hc01 30,277,542(dyna2) | hc02 11,099,145(dyna1) | hc03 55,846,482(serial) | hc04 727,171,564(dyna2). Optimization now in marginal-gains regime (~0.02-0.1%/refinement); the big win (dynasearch crossing 748K) is done.

**THE BLOCKER IS TRIAL-EVALUATION THROUGHPUT.** Each candidate bit-swap = 4 `incrAccurateRecomputeFF` calls (apply A+B, revert A+B), fully serial. The fix = scite #2 **cone-disjoint dynasearch + a side-effect-free parallel `evalBitSwapDelta`**: bit-repair swaps are same-cell (QpinDelay identical) and only change cfa/cfb D/Q pin positions (computable as `B.getNewCoor()+B.getPinCoor("D"+to_string(sb))`; pin name = `to_string(slot)` per FF::getPhysicalPinName), so a read-only delta oracle is feasible -> thread-safe `#pragma omp` over candidates + 1 cone-walk instead of 4 -> ~8-16× throughput -> bit-repair converges within production budget. Decisive ceiling experiment IN PROGRESS: single-swap depth0 @ 800s/40rounds on tc2 — if it reaches ~746K the dynasearch refactor is justified with a clear target.

**Conclusion (SUPERSEDED — see BREAKTHROUGH/INCREMENTAL above; multi-hop IS the faithful metric, not 1-hop):**
NTU thesis Table 4.6 (tc2 per-stage) is decisive: NTU's banked+legalized layout has TNS **14,370** (WORSE than our 7,850); they win tc2 entirely in ONE post-legalization **Refinement** step that cuts TNS 14,370→5,309 (−63%) at FIXED power+area (−9.1% score). NTU uses the SAME contest 1-hop slack model as our getSlack (thesis p28; contest spec P2 L288-292: `S' = S + (δ0-δ0') + DispDelay·(WL_Q changes + WL_D changes)`). So **the gap is NOT model fidelity / not the oracle** — multi-hop STA would mis-price (explains ACCURATE_BANKING 775K regression: it charges delay the evaluator never sees). The gap is the **ABSENCE of a post-legalization critical-path FF-swap refinement pass** (NTU §3.3 step 1: swap critical FFs with nearby legal FFs/sites, accept if 1-hop ΔTNS<0). We have ZERO equivalent (DP GlobalSwap/ChangeCell is WL/cost-driven; EGR only debanks). This is the post-MERGE PLACEMENT layer all ~40 prior merge/threshold configs never touched.
Two refinements of our own internal cost (separate, smaller than the oracle): our getTNS=5,864 understates eval 7,850 because getSlack charges displacement on a FROZEN single critical path (Gate::setMaxInput records one max-cost predecessor at ORIGINAL positions, never re-selects max-over-inputs at current positions) — a ~3-day "re-select critical path + required-time anchor R(f)=getTimingSlack(D)+BFS_orig_arrival" fix makes the 1-hop metric faithful WITHOUT multi-hop. That faithful 1-hop metric is what should SCORE the refinement swaps.
**Recommended path (days, not weeks):** (1) port NTU's critical-path FF-swap Refinement pass [HIGHEST ROI, the actual tc2 mechanism], scored by (2) a cheap faithful 1-hop slack (re-select critical path). Backup = cohesive NTU 5-step port (Track B in [[plan_two_track_beat_top3.md]]). Oracle (Method 1) DEMOTED — model fidelity isn't the differentiator. Best tuning backstop remains RELOC+EGR −0.13%.
