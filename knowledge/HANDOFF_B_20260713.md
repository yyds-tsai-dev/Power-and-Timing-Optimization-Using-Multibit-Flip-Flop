# Server B 交接文件(2026-07-13 03:30,session 切換點)

> **⚡⚡⚡⚡⚡⚡ 2026-07-14 13:20 六次更新:v4 生效,收官態**
>
> - **v4 儀式完成**:rep2 七案逐位同 rep1、det×2 全逐位、T128 三案逐位同
>   → 提案生效,**README v4 欄已上(composite 0.9428)**。
> - chain9 定案:LNS_TIME/margin/K=2 全無效(W1 配置最終);tc1 conv knobs
>   證偽(三配置同分)→ tc1 殘餘 +100k gap 查 ADAPT_STACK(下一 session)。
> - **EJECT_ROWFIX 已落庫並驗證**(gated,**修正版 3160643b**;首版 4cb86161
>   有 byte-exact bug 已修——見下)。驗證 `reports/2026-07-16_exp_eject_rowfix.md`:
>   P0 gate-off byte-exact PASS、P1 gate-on 6/7 逐位同、tc1 同分不同位元(合法)。
>   **判決:潛伏雷真實但賽題集良性 → 保持 gated default-off,不併入凍結 v4。**
> - **⚠️ 教訓入 memory** [[server-b-byte-exact-pointer-hash-trap]]:新 gate 首版
>   把 `rowedNew.insert()` 寫在 gate 外 → gate-off 仍配置堆 → 指標鍵 unordered
>   迭代序漂移 → tc1 同分不同位元。**新 gate 每一行(連無害容器插入)都要
>   gate-guard;byte-exact 驗收看到「同分不同位元」= 指標序漂移,不是噪音。**
> - **rowfix 終驗(2026-07-16)**:gate-on 獨跑 tc1 三方逐位同(d1≡d2≡rep1)
>   → gate-on 全綠。chain10 P1 tc1 當次漂移翻案為**先天罕見負載 race**
>   (postBanking→RELOC 段;~50+ 跑動僅 1 咬;同分合法;env-size 與 rowfix
>   皆對照排除)。完整解剖 `reports/2026-07-16_exp_eject_rowfix.md` §3b。
> - **tc1 record 調查收線(2026-07-16)**:B 側全假說排除(六時點 binary 掃
>   零漂移、no-EJECT 反向、T8 同分、v4 knobs 全無辜——conv 還幫 −476)。
>   **B 無法重現 734,266,279;剩「A 的 WIP 樹工件」vs「跨機分歧」,
>   A 一發 tc1 定案**(加冕包已加必跑項)。診斷
>   `reports/2026-07-16_diagnosis_tc1_v2_record_provenance.md`。
> - **helgrind races=0** → 1/50 翻位嫌疑精煉為 OMP 動態排程 FP 累加序
>   (定序化修法排隊,gated)。
> - 待辦隊列:FP 累加序定序化(gated)、LNS Phase C/D、PROBE v5(per-case)、
>   等 A 的 tc1 一發 + 加冕(v3+v4)、de-hashing(A)。
>
> chain7 矩陣收全(數據經獨立重算通道核對)→
> **`reports/2026-07-14_v4_freeze_proposal.md`:主推 v4 = v3 +
> REBANK_MODES=15 + LNS K=4(W1),七案全勝 v3,composite 0.9428;
> PROBE 不進 v4(hc02 逆風)、進 v5 菜單。**
> 尚在跑:W1 det×2(hc02 det1 已 ✓;全綠後提案生效,CHAIN7_DONE 為記)。
> 下一步:v4 儀式 B 側 2×(v3 先例:rep2 逐位同免 rep3)→ README v4 欄。
>
> **⚡⚡⚡⚡ 2026-07-13 23:35 四次更新:chain5/6 判讀完畢,chain7 過夜**
>
> - chain5/6 結果與判讀全在 `reports/2026-07-13_exp_lns_phaseA_probe.md`:
>   margin M=0 定案、**K=4 甜點(hc02 −0.385%)**、R1+LNS = 最佳單一配置
>   (composite 0.9430)、hc04 正協同、hc01 勿開 LNS、PROBE 工程全綠
>   (攪動 −60%、tc3 解鎖 10 accepts)但 hc02 上是分數逆風。
> - **chain7 過夜中**(23:29 起,`~/scratch/chain7_20260713.log`):K=4
>   四波矩陣(W1 R1+LNS / W2 R1+LNS+probe / W3 LNS+probe / W4 LNS)+
>   W1 det×2。判讀指南:每案挑四波與 R1-only(r1v3.log)的最優 →
>   **per-case v4 配方定案** → 寫 v4 凍結提案。預估 composite ~0.9428。
> - 教訓入庫:pkill -f 會自噬(匹配到自己的 shell)——砍程序用明確 PID。
> - 之後隊列:EJECT revertAll rowfix(gated)、K=2 探底、Phase C/D。
>
> **裁定(使用者)**:不等 A 的跨機加冕,B 側四軸自證(重跑/執行緒/負載/
> **build**——BI 套件七案全逐位,含 build 軸)。加冕包仍在
> `knowledge/coronation/` 供 A 有空時用。
>
> **今日落庫**:v3 儀式 B 側收官(免 rep3)、GPU 實驗兩案定稿、T128 定案
> (名單 tc3/hc04/hc03;判準證偽記錄在案)、tc1 階梯、R1×v3 疊加 6勝1平、
> **LNS v1.5 落庫 `ac71ff3`(hc02 −0.252%,驗收全綠)** + row-discipline
> postmortem、**LNS_PROBE v2 Phase B 落庫(real-site 定價 + name-clean
> reject)**、README v3 欄(composite 0.9437)。報告七份,全部已 push。
>
> **晚間自動管線(全 setsid+nohup,斷線照跑)**:
> chain5(執行中,`~/scratch/chain5_20260713.log`):A1 margin{50,200,1000}
> → A2 K{3,4} → A4 LNS 七案 gate(K=6)→ A3 R1+LNS 疊加七案。
> chain6(排隊,`~/scratch/chain6_20260713.log`):PROBE byte-exact 抽查 →
> hc02/tc3 probe 版(tc3 逐位同 rep1 = zero-churn 證明)→ det×2 →
> 條件式七案 gate。預計 ~21:30 全完。
> **下個 session**:讀兩個 chain log → 判 A3 疊加是否過 0.5% gate(過了
> v4 凍結候選 = v3+R1+LNS 證據齊)→ 補 LNS 報告 P4/A 系欄位 → v4 凍結
> 提案。INCR_VALIDATE 在 anchor 模式的 44 條 diff = 既有基線(對照已證),
> 勿當 bug 追。

> **⚡⚡ 2026-07-13 05:50 二次更新:三項優先序全部完成**
>
> - **① GPU 實驗完結**:報告 `reports/2026-07-13_exp_gpu_screening_benchmark.md`
>   (正確性 ✅ maxErr~1e-11;FP32 召回 100%@M=2K;GPU kernel 掃描 bound →
>   生產路線 = CPU flat + cone 分流)。
> - **② T128 完結**:報告 `reports/2026-07-13_exp_t128_probe.md`(tc3 −17.7% /
>   hc02 +7.4%,皆逐位同 rep1;判準 = T64 飽和度;hc04_T128 補測執行中)。
> - **③ rep2 完結:7/7 逐位同 rep1 → 免 rep3(裁定生效)**。B 側儀式收官,
>   報告 `reports/2026-07-13_exp_v3_ritual_b_side.md`(14-run 表 + 三維
>   determinism 證據鏈)。等 A 的 3× → 跨機對帳加冕。
> - **LNS v1.5 已實作**(`LNS_SPLIT` gate,default off;bit 拆分 destroy =
>   debankWithUndo 組合 + 跨界 bankFF + EJECT 式萬用還原;screening 用整區
>   一次 evalRemapDelta)。代碼在 working tree(src/Manager.cpp),**等
>   byte-exact-off PASS 才 commit**。驗收批次 `~/scratch/lns15/lns15_batch.sh`
>   排隊中(hc04_T128 完→自動跑:tc2 byte-exact → hc02 v1.5 smoke →
>   hc02 v1 對照;結果 `~/scratch/lns15/lns15.log`)。
> - 環境備忘:本機 NFS 對「他程序剛寫的檔」讀取會嚴重滯後(分鐘級),
>   monitor/tail -F 持有 fd 才即時;判狀態以 ps 為準,勿信剛寫的 log tail。
> - **chain3 已排(使用者指示塞滿到 ~12:00)**:`~/scratch/chain3_20260713.sh`
>   (log 同名 .log)。LNS 批次完 → A) hc03_T128 證偽測試 + tc1 執行緒階梯
>   (8/16/32/64/128,逐檔 cmp rep1)→ B) **R1×v3 疊加**七案(REBANK_MODES=15
>   on 凍結 env,`~/scratch/r1v3/r1v3.log`,v4 菜單關鍵問題)→ C) LNS v1.5
>   全套(條件:byte-exact PASS + smoke sanity 綠;INCR_VALIDATE 自檢、
>   determinism ×2、K∈{6,16}+T600 掃、tc1/tc2、**七案 gate** @TIME=120,
>   全在 `~/scratch/lns15/lns15.log`)→ D) GPU hc02 dump+bench 泛化檢查
>   (>11:45 自動跳過)。判讀順序與 0.5% gate 規則照 CLAUDE.md。
>
> **⚡ 2026-07-13 04:25 進度更新(session 執行中,斷線窗前落筆)**
>
> **① GPU 實驗已跑完第一輪**:dump 成功(`gates=132293 bits=21164 cands=566044
> cpuScreen64T=9.553s`,檔在 `~/scratch/gpu_proto/tc2gpu.snap/.cand`)。
> bench v1 結果(`gpubench_result.txt`):CPU flat 1T 238.2s **maxErr=1.6e-11
> → 平坦化正確性關卡 PASS**;CPU flat 160T 4.49s(126k cand/s);GPU FP64 173.4s
> maxErr=1.3e-11(0.03× vs 160T,手冊預期內);**spill=50485(8.9%)超標** →
> 已做 v2(`tools/gpu_screen_bench_v2.cu` + `gpu_screen_bench_v2`):cap 加倍
> 4096/8192、OMP 段補 64T(乾淨 de-hash 對照)+160T、**新增 GPU FP32 段 +
> top-K 召回率/sign-miss 指標**(生產路線判準)。
> ⚠️ 異象記錄:v1 執行期間 monitor 曾撈到一組不同數字(1T 391s/maxErr 8.9e-10/
> spill=0/64T)來源不明(疑似重複啟動或殘留快取);以 `gpubench_result.txt`
> 落檔版為準,v2 乾淨重跑後以 v2 為權威。
>
> **過夜鏈執行中**(04:33 改版 `~/scratch/chain2_20260713.sh`,使用者指示並行化;
> 舊 chain_20260713.sh 已停,bench v2 保留續跑):
> bench v2 CPU 段獨佔跑完 → **T128 單獨跑**(牆鐘探測不容污染;bench 殘餘 GPU
> 段僅佔 1 核)→ **rep2 兩波並行 64T 車道**(tc3∥hc04,再 hc02∥hc03→hc01;
> 正當性:rep1 已證 tc2 8T/64T 逐位相同 → 執行緒排程不影響輸出;rep2 wall
> 有噪音屬預期,分數才是判準)。自動 cmp rep1,ritual.log 寫
> BYTE-IDENTICAL/DIFFERS。進度看 `~/scratch/chain2_20260713.log`
> (CHAIN2_DONE = 全完,預估 ~05:40);結果檔:`gpu_proto/gpubench_v2_result.txt`、
> `v3_ritual/t128.log`、`v3_ritual/ritual.log`。
> **下個 session**:讀上述三檔 → 判 rep3 要不要跑(五案全 BYTE-IDENTICAL 即免)
> → 寫 GPU 實驗報告 + T128 結論 + B 側 21-run 表(§3)。

**新 session 開機:先讀本檔 → `BOOTSTRAP_DAC2027.md` → `2026-07-12_revival_finale.md`。**
本檔只講「此刻在飛什麼、下一步做什麼」;歷史與設計在上述兩檔。

---

## 0. 一句話狀態 + **執行優先序(使用者 2026-07-13 指示)**

**① GPU 實驗 → ② T128 探測 → ③ rep2 補完(若 rep2 逐位同 rep1,則不跑 rep3)。**

v3 儀式**已暫停**(rep1 7/7 全綠;rep2 已跑 tc2/tc1,兩案皆**逐位同 rep1**)。
背景任務全部已停,機器空著。GPU 原型寫完編過但**尚未執行**——
手冊:`plans/plan_gpu_screening_experiment.md`(照那份跑)。
所有主線成果已 commit push。

---

## 1. 三個待辦(按優先序,機器目前空著)

### ① GPU 篩選實驗(**先做這個**)
照 `plans/plan_gpu_screening_experiment.md` 跑——那份是完整執行手冊
(指令、判讀標準、預期管理、風險)。重點提醒:
- 代碼全部備好(worktree `~/scratch/gpu_proto`,dump 鉤子 + CUDA bench 都編過)
- **L4 的 FP64 是 1/64 速率;FP64 版輸給 64T CPU 是正常的,不是失敗**。
  生產路線是 FP32 篩選 + CPU exact 重報價(手冊 §3 有完整論證)
- 先看 `maxErr`(正確性關卡)再看吞吐量

### ② T128 探測
`bash ~/scratch/v3_ritual/t128_probe.sh`(已寫好:tc3 + hc02,唯二有 >40 核胃口的案)
→ 結果進 `~/scratch/v3_ritual/t128.log`。對照組 = 儀式的 T64 同案數據
(tc3 1131s / hc02 567s)。問題:128T 是否還有收益,或 NUMA 跨插槽 + barrier
空等吃光(4 socket,8T 時 barrier 已佔 17%)。

### ③ rep2 補完(最後做)
`bash ~/scratch/v3_ritual/ritual.sh` **會從頭跑 21 runs**——若只要補 rep2 剩下的
五案(hc02/hc03/tc3/hc01/hc04),改 `for rep in 1 2 3` 為 `for rep in 2`
且案序去掉已完成的 tc2/tc1,或直接單案手跑(env 見 `env_dump.txt`)。
**判準:rep2 若與 rep1 逐位相同 → 不跑 rep3**(使用者指示)。
已知:tc2_r2 與 tc1_r2 皆逐位同 rep1 ✓

**長跑紀律**:過夜用 `nohup`(memory 有記:清晨 4:30–5:50 連線必斷)。

## 2. v3 儀式 — rep1 結果(B 側,T64,unified v2 + CONV_TERM + ADAPT_STACK)

| Case | rep1 分數 | 軸/機制 | wall |
|---|---:|---|---|
| tc2 | 705,140.829646587 | stack ON(tns_share=0.1455)| 337s |
| tc1 | 734,700,023.359553 | stack off;conv −476 | 100s |
| hc02 | 9,678,012.79050386 | stack ON,−4.28% | 567s |
| hc03 | 55,771,346.6806144 | off,= v2 base | 389s |
| tc3 | 725,803,320.322809 | conv 深收斂 −123k | 1131s |
| hc01 | 29,993,040.5219705 | off,= v2 base | 98s |
| hc04 | 725,980,967.603273 | conv 深收斂 −103k | 1266s |

**ADAPT 四項驗收全過**(A 的開跑令):軸讀數 ON + 重啟 + 分數落帶
(tc2 705,140 與 8T 疊加矩陣**逐位相同**)+ checker 綠。
tc1 反向驗證亦過(stack=off、無重啟)。

**環境限制(要在報告註明)**:`checker/placement_checker` 在 B 機**無法執行**
(要 GLIBCXX_3.4.29,本機 glibc 2.28)。合法性以 **sanity + evaluator 內建
placeViol**(反組譯證據:score() 計分前強制跑 die/site/overlap 檢查,
非零分數即證合法)。A 側有原生 placement_checker 雙保險。

## 3. 儀式收尾要做的事(下一個 session 的主線)

1. 等 `RITUAL_DONE`(~2h)→ 出 **B 側 21-run 表**:per-case 中位數 + 3× 內部穩定性
   (預期:OFF 案逐位同;ON 案與撞牆案有窗口噪音)+ wall(標 T64)。
2. **跨機對帳**:A 補完他的 3×(他機器超載中,等安靜窗口)→ B 彙整兩機 42 runs
   → **跨機一致性表 = 儀式的加冕判準**(凍結清單:跨機分數一致即 determinism 主張成立)。
3. 加冕通過 → 寫 v3 report + **更新 README 表**(加 v3 欄,現有 v2 欄保留作對比;
   README 已在 main 更新過一次,commit `3469d46`)。
4. 預估 composite ~0.941(對照 v2 = 0.9500)。

## 4. GPU 加速實驗(**已備好,等鏈跑到**)

- **分支/worktree**:`gpu_screening_proto` @ `~/scratch/gpu_proto`(基於 d182ba1,
  **獨立分支,不碰主線**)。
- **dump 鉤子**:`GPU_DUMP=<prefix>` env(掛在 `bitRepairRefine` 的 oracle build 後)
  → 把凍結快取平坦化成稠密陣列(gate id = topo rank)+ 枚舉一批 bit-swap 候選
  + 用 `evalBitSwapDelta` 算 CPU 真值 → 寫 `<prefix>.snap/.cand` 後 exit。
- **benchmark**:`tools/gpu_screen_bench.cu`(已用 nvcc sm_89 編過 → `./gpu_screen_bench`)。
  三方對照:CPU-flat 1T / CPU-flat 64T / GPU FP64,逐候選對真值驗誤差。
- **預期管理**:L4 的 FP64 是 1/64 速率 → FP64 版可能輸給 64T CPU,**這不是失敗**;
  生產路線是 **FP32 篩選 + CPU exact 重報價**(apply 時本來就重驗,架構天然容錯)。
  FP32 kernel 是 benchmark 跑通後的第二步(模板已泛型化,改 `kScreen<float>` 即可)。
- 機器:NVIDIA L4 24GB + CUDA 12.1/12.2/12.5,**共用**(常被他人佔滿,看 nvidia-smi)。

## 5. B 側其他隊列(不阻塞)

1. **LNS v1.5**(bit 拆分 destroy)——真 novelty,施工圖 `plan_lns_destroy_repair.md` §2;
   v1 已綠落庫(hc02 −0.034%,move space 多被 rebank 收割,如預期)。
2. **R1 已 gate 完畢 7/7 全勝**(`2026-07-13_r1_gate_results.md`;tc2 −0.333%)——
   進 v4 菜單,與 ADAPT_STACK 正交,疊加測試排 v4。
3. tc1 乾淨 wall 階梯(等機器安靜);de-hashing 是 A 的。

## 6. 與 A 的協議現況

- 圍欄:A 擁 Manager.cpp 主幹(conv-term/ParamMgr/oracle 家族);B 擁
  oracleRebankRefine / postLGResynth / DetailPlacement + 全部實驗分析。
- 紅旗(HEAD 漂移)**已結案**:A 的基線漏掛 `ORACLE_EJECT`,非代碼問題
  (B 的六點 bisect 證 e7df424→HEAD 逐位零漂移)。教訓:**基線宣稱必附 env diff**。
- v3 凍結清單:`2026-07-13_v3_freeze_list.md`(env set、執行緒各機自由、
  R1/LNS/de-hashing 不進 v3)。

## 7. 工件索引(server B `~/scratch/`)

`v3_ritual/`(儀式)、`gpu_proto/`(GPU 原型)、`bisect/`(六時點 worktree+binary)、
`probes_r1/` `probes_r2b/` `probes_r2c/` `probes_stack/` `probes_lns/`(翻案實驗)、
`verify_1bis/` `tns_share/` `run_3b/` `refsta/`(驗收與分析)。
